# Copy to .env and adjust as needed: `cp .env.example .env`

# Host port the public gateway is published on (container always listens on 8080).
GATEWAY_PORT=8080

# RagnaPlace public API key (used by the mobs.json stats scrape).
RAGNAPLACE_API_KEY=
