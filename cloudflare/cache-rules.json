{
  "$comment": [
    "The http_request_cache_settings ruleset for assets.latam-tools.com.br.",
    "",
    "DORMANT since 2026-09-09. That hostname is grey-clouded (DNS only), so no",
    "request for it reaches Cloudflare and not one of these rules can match. They",
    "are kept, not deleted, because flipping the record back to orange is the",
    "rollback plan for the direct-serving experiment, and without them Cloudflare",
    "Free would silently stop caching renders again — /image has no file",
    "extension, which is the whole reason rule 2 exists. Applying this file is",
    "still safe and still correct; it just has no effect on live traffic.",
    "",
    "Applied by tools/apply-cloudflare.mjs, which PUTs this whole array to the",
    "phase entrypoint. That endpoint replaces the ruleset wholesale, so this file",
    "is the complete desired state: a rule deleted here is deleted in Cloudflare on",
    "the next apply. There is no state file and no drift to reconcile.",
    "",
    "EVERY rule must test http.host. This zone also serves latam-market, short,",
    "simulador-latam-ro and latam-social; a rule without the host check would",
    "silently change caching for all of them.",
    "",
    "Order matters — Cloudflare evaluates top to bottom and the last match wins for",
    "a given setting, so the bypass rule comes first."
  ],
  "rules": [
    {
      "description": "ragassets: never cache the help page or the health check",
      "expression": "(http.host eq \"assets.latam-tools.com.br\" and (http.request.uri.path eq \"/\" or http.request.uri.path eq \"/healthz\"))",
      "action": "set_cache_settings",
      "action_parameters": {
        "cache": false
      }
    },
    {
      "description": "ragassets: renders — the URLs Cloudflare would not cache on its own",
      "$comment": [
        "This rule is the reason the migration is worth anything. Cloudflare Free",
        "decides what to cache by file extension, and /image?job=1002&action=0 has",
        "none — so the most expensive responses on the site are precisely the ones",
        "it would skip. On the old Worker route the Worker ran in front of the cache",
        "and renders were never stored at all (no CF-Cache-Status header in",
        "production, re-rendered every request).",
        "",
        "The query string IS the identity of a render, and Cloudflare's default",
        "cache key already includes it in full — so there is deliberately no",
        "cache_key override here. An earlier version set one 'to be explicit' and",
        "the API rejected it: custom cache keys are Enterprise-only. It was also",
        "redundant, which is the more useful half of that lesson.",
        "",
        "Note api.ETagFor hashes the query order-independently while the cache key",
        "does not: ?job=1&action=0 and ?action=0&job=1 are two edge entries holding",
        "identical bytes. Extra misses, never wrong bytes."
      ],
      "expression": "(http.host eq \"assets.latam-tools.com.br\" and (starts_with(http.request.uri.path, \"/image\") or starts_with(http.request.uri.path, \"/gif\")))",
      "action": "set_cache_settings",
      "action_parameters": {
        "cache": true,
        "edge_ttl": { "mode": "respect_origin" },
        "browser_ttl": { "mode": "respect_origin" }
      }
    },
    {
      "description": "ragassets: assets and data tables",
      "$comment": [
        "Most of these carry a file extension and would cache anyway. The rule",
        "exists for the query-keyed /effect/* endpoints, and so the whole policy is",
        "stated in one place rather than half-implicit in Cloudflare's extension",
        "list.",
        "",
        "respect_origin keeps the split the Go server already makes: immutable for",
        "assets, max-age=300 must-revalidate for /raw. Do not flatten these into a",
        "fixed TTL — three sibling projects poll /raw/items.json on every client",
        "update and depend on revalidating."
      ],
      "expression": "(http.host eq \"assets.latam-tools.com.br\" and (starts_with(http.request.uri.path, \"/icons/\") or starts_with(http.request.uri.path, \"/illust/\") or starts_with(http.request.uri.path, \"/effects/\") or starts_with(http.request.uri.path, \"/effect/\") or starts_with(http.request.uri.path, \"/maps/\") or starts_with(http.request.uri.path, \"/bgm/\") or starts_with(http.request.uri.path, \"/raw/\")))",
      "action": "set_cache_settings",
      "action_parameters": {
        "cache": true,
        "edge_ttl": { "mode": "respect_origin" },
        "browser_ttl": { "mode": "respect_origin" }
      }
    }
  ]
}
