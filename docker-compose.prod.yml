# Production deployment: the Go gateway (internal) behind a Caddy reverse proxy
# that terminates TLS with an automatic Let's Encrypt certificate.
#
#   docker compose -f docker-compose.prod.yml up -d --build
#
# Rendering is in-process (no zrenderer container). Only Caddy is published to the
# host (80/443); the gateway is reachable only on the internal network as
# `gateway:8080`. Point your domain's A record at this host's public IP before
# starting, so Let's Encrypt can validate.

services:
  gateway:
    build: ./gateway
    environment:
      RESOURCE_DIR: "/resources"
      ICONS_DIR: "/icons"
      EFFECTS_DIR: "/effects"
      MAPS_DIR: "/maps"
      BGM_DIR: "/bgm"
      GATEWAY_PORT: "8080"
      # Cap Go's heap target so the runtime runs GC hard before the container hits
      # its mem_limit. Prod EC2 has only ~900 MiB total; without this the process
      # grew to ~350 MiB RSS over ~24 h and the kernel OOM-killed it.
      GOMEMLIMIT: "500MiB"
    mem_limit: 600m
    volumes:
      - ./resources:/resources:ro
      - ./resources/icons:/icons:ro     # static icons from extract-grf.mjs --icons (optional)
      - ./resources/effects:/effects:ro # effect-only costume bundles from extract-grf.mjs --effects (optional)
      - ./resources/maps:/maps:ro       # world-map bundles from extract-grf.mjs --maps (optional)
      - ./resources/bgm:/bgm:ro         # per-map background music from extract-grf.mjs --bgm (optional)
    expose:
      - "8080"
    restart: unless-stopped

  caddy:
    image: caddy:2-alpine
    depends_on:
      - gateway
    ports:
      - "80:80"     # ACME HTTP-01 challenge + http→https redirect
      - "443:443"   # public HTTPS
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data      # persists issued certificates
      - caddy-config:/config
    restart: unless-stopped

volumes:
  caddy-data:
  caddy-config:
