# Normalize line endings to LF. These files are read inside Linux containers
# (zrenderer.docker.conf, Dockerfile, *.go); CRLF could corrupt config values.
* text=auto eol=lf
*.png binary
*.spr binary
*.act binary
*.grf binary
