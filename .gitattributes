# Normalize line endings to LF. These files are read on the Linux origin box
# (Caddyfile, systemd units, shell scripts, *.go); CRLF could corrupt config values.
* text=auto eol=lf
*.png binary
*.spr binary
*.act binary
*.imf binary
*.pal binary
*.grf binary
*.gpf binary
